apiVersion: v1
kind: Service
metadata:
  name: hadoop-master
  labels:
    app: hadoop-master
spec:
  ports:
    - port: 50070
      name: hadoop-master
    - port: 8088
      name: hadoop-master
  selector:
    app: hadoop-master
